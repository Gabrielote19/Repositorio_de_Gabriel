/* Uso de la sentencia printf() 1. */

#include <stdio.h>

main() /*Saca por pantalla una suma */
{
    int a=20,b=10;
    printf("El Valor de a es %d\n",a);
    printf("El Valor de b es %d\n",b);
    printf("Por lo tanto %d+%d=%d",a,b,a+b);
}